/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package chapter4;

/**
 *
 * @author Daniel Haqeem
 */
/*import java.util.Scanner;
public class testidentifier {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        
        //Input number of students
        System.out.print("Enter the number of students: ");
        int numStudents = scanner.nextInt();

        int[]testResults = new int[numStudents];

        System.out.println("Enter the test results (0 for fail, 1 for pass):");
        for (int i = 0; i < numStudents; i++) {
            System.out.print("Student number "+ (i+1) + " : ");
            testResults[i] = scanner.nextInt();
        }

        int numPassed = 0;
        int numFailed = 0;

        for (int result : testResults) {
            if (result == 1) {
                numPassed++;
            } else {
                numFailed++;
            }
        }

        System.out.println("Number of students who passed: " + numPassed);
        System.out.println("Number of students who failed: " + numFailed);

        double passRate = (double) numPassed / numStudents * 100;
        if (passRate > 50) {
            System.out.println("Bonus to instructor!");
        }
    }
}*/
    
    

